# CommonADK — Task Log

All actions taken on this project are logged here. Planning lives in `plan.md`.

## Milestone status

- [x] **M1 — Core**: models, loader, validation, mermaid renderer, example, tests (31 tests passing)
- [x] **M2 — Google ADK adapter** (43 tests passing; entry agent builds full tree)
- [x] **M3 — OpenAI Agents adapter** (hypothesis test PASSED: same `common/` builds on both SDKs; 53 tests)
- [x] **M4 — CLI & docs** (`commonadk validate|render|run` + README; 64 tests passing)

- [x] **M5 — Claude Agent SDK adapter** (79 tests; hypothesis test now spans google-adk/openai/claude)
- [x] **M6 — CrewAI adapter** (92 tests; hypothesis test spans 4 targets)
- [x] **M7 — AutoGen adapter** (107 tests; hypothesis test spans 5 targets)
- [x] **M8 — LangGraph adapter** (124 tests; hypothesis test spans all 6 targets)

## Action log

| Date | Actor | Action |
|---|---|---|
| 2026-08-18 | Orchestrator | Planning session: hypothesis, mechanism (runtime factory), interaction source of truth (YAML → generated mermaid), v1 targets (Google ADK + OpenAI Agents), LiteLLM model layer, `requires.env` contract settled |
| 2026-08-18 | Orchestrator | Wrote `plan.md` and `tasks.md`; committed to `claude/project-planning-hypothesis-qopc3j` |
| 2026-08-18 | Orchestrator | Spawned Sonnet 5 subagent to implement M1 |
| 2026-08-18 | Orchestrator | Roadmap added to `plan.md`: mixed-target spawning (per-agent runtime pinning, future); v1 stays single-target per build; reserved `runtime:` key in agent-config schema |
| 2026-08-18 | Sonnet 5 subagent | Implemented M1: `commonadk` package (models, loader, validation, mermaid renderer), `examples/research-crew`, 28 tests passing |
| 2026-08-18 | Orchestrator | Reviewed M1: approved core; flagged missing reserved `runtime:` key and silent unknown-YAML-key acceptance |
| 2026-08-18 | Sonnet 5 subagent | Follow-up: added `runtime:` reservation warning and `extra="forbid"` on YAML-facing models; 31 tests passing |
| 2026-08-18 | Orchestrator | Independently reran tests + smoke-tested load/alias-resolution/check_env; committed and pushed M1 |
| 2026-08-18 | Sonnet 5 subagent | Implemented M2: adapter registry (lazy imports), GoogleADKAdapter (native gemini vs LiteLlm routing, env preflight, multi-parent/cycle detection), `Project.build()`, 12 new tests; added `litellm` to google extra (ADK's LiteLlm wrapper hard-requires it) |
| 2026-08-18 | Orchestrator | Reviewed M2: adapter approved; flagged example graph (writer had two parents — unbuildable on Google ADK, would break M3 hypothesis test) |
| 2026-08-18 | Sonnet 5 subagent | Follow-up: example reshaped to clean tree (coordinator→researcher→writer), interaction-layer.md regenerated, multi-parent/cycle coverage moved to fixtures |
| 2026-08-18 | Orchestrator | Independently reran suite (43 passing) + smoke-built coordinator tree on google-adk; committed and pushed M2 |
| 2026-08-18 | Sonnet 5 subagent | Implemented M3: OpenAIAgentsAdapter (native openai vs LitellmModel routing, memoized shared instances for multi-parent graphs, cycle-safe two-pass wiring), env preflight hoisted to BaseAdapter, 10 new tests incl. `test_same_project_builds_on_both_targets` |
| 2026-08-18 | Orchestrator | Reviewed M3: approved; independently reran suite (53 passing) and live-verified the hypothesis — one Project builds coordinator on google-adk (sub_agents tree) and openai (handoff graph) from the same `common/`; committed and pushed |
| 2026-08-18 | Sonnet 5 subagent | Implemented M4: argparse CLI (`validate`/`render`/`run`/`--version`, lazy SDK imports, clean error paths, warnings surfaced), console script, 11 CLI tests, README rewritten from stub into full docs |
| 2026-08-18 | Orchestrator | Reviewed M4: approved; independently reran suite (64 passing) and exercised the CLI (validate summary, unknown-target error, version); committed and pushed — v1 plan (M1–M4) complete |
| 2026-08-18 | Sonnet 5 subagent | Wrote docs/: README (index), HLD.md, LLD.md, file-contracts.md — all cross-checked against source (field tables from pydantic models, error strings matched verbatim, examples copied from shipped project) |
| 2026-08-18 | Orchestrator | Reviewed docs; fixed stale "once the CLI lands" note in mermaid.py's generated header (flagged by docs review) and regenerated example interaction-layer.md via `commonadk render`; committed and pushed |
| 2026-08-18 | Sonnet 5 subagent | Implemented M5: ClaudeAgentSDKAdapter (ClaudeAgentOptions with flat subagent registry + Agent-tool edges, per-agent in-process MCP tool servers, anthropic-only model routing with clear error), CLI claude branch, example claude overrides, hypothesis test parametrized over 3 targets |
| 2026-08-18 | Orchestrator | Reviewed M5: approved; verified subagent tool isolation, Agent-tool granted only to agents with outgoing edges, non-anthropic error path; 79 tests passing; committed and pushed |
| 2026-08-19 | Sonnet 5 subagent | Implemented M6: CrewAIAdapter (always returns a Crew — hierarchical with root as manager_agent when reachable agents exist, solo sequential for leaves; LiteLLM-native model pass-through with temperature/max_tokens; crew-wide delegation coarsening documented; manager tools dropped with warning per CrewAI's own constraint), CLI crewai branch, 12 new tests |
| 2026-08-19 | Orchestrator | Reviewed M6: approved; verified crew shapes, delegation flags, model routing live; noted pip dependency tension (crewai pins openai<3, openai-agents prefers >=3 — resolved to openai 2.54.0, all 92 tests still pass); committed and pushed |
| 2026-08-19 | Sonnet 5 subagent | Implemented M7: AutoGenAdapter (Swarm of reachable agents with root as initial speaker, leaf builds return bare AssistantAgent; openai/anthropic/gemini model clients with explicit model_info to dodge autogen-ext's broken fuzzy model tables; string-name handoffs make multi-parent/cycles trivial), CLI autogen branch, 14 new tests |
| 2026-08-19 | Orchestrator | Reviewed M7: approved; verified Swarm/leaf shapes and tool wiring live; example builds unmodified on autogen (no overrides needed); noted protobuf downgrade by autogen-core (suite unaffected); committed and pushed |
| 2026-08-19 | Sonnet 5 subagent | Implemented M8: LangGraphAdapter (compiled StateGraph of react-agent nodes with per-edge transfer_to_<agent> handoff tools via Command(goto=...) — the only target honoring edge targets precisely; init_chat_model routing for gemini/openai/anthropic; used create_agent, not the deprecated create_react_agent), CLI langgraph branch, 16 new tests |
| 2026-08-19 | Orchestrator | Reviewed M8: approved; verified graph nodes and leaf shape live; example builds unmodified; no new pip pins needed; committed and pushed — v2 adapter expansion (M5–M8) complete, 6 targets total |
| 2026-08-19 | Sonnet 5 subagent | Refreshed docs/ for all six targets: HLD comparison table + edge-fidelity spectrum as core finding, LLD subsections for the four new adapters + updated registry/CLI/error-taxonomy/testing sections, file-contracts per-target override forms, index updated; also fixed stale captions and milestone refs |
| 2026-08-19 | Orchestrator | Reviewed docs refresh: approved; corrected one inaccuracy (LLD quoted the pre-M4-fix mermaid header text as current); committed and pushed; updated PR #2 description to six-target scope |
| 2026-08-27 | Orchestrator | Branch restarted from merged main (PRs #2 and #3 both merged); pushed restart point |
| 2026-08-27 | Sonnet 5 subagent | Docs audit (fixed stale two-SDKs wording; added multi-extra install note with verified pip tensions) + demo runs: examples/demo.py (offline, builds all six targets, labeled error demonstrations), docs/demo-runs.md (real captured output only), tests/test_demo.py; 126 tests passing |
| 2026-08-27 | Orchestrator | Reviewed: reran suite (126) and demo (exit 0, all six targets OK); committed, pushed, opened PR |
| 2026-08-27 | Sonnet 5 subagent | Added repo visuals: assets/banner.png (1280x640, GitHub social-preview size) + assets/demo.gif (14-frame terminal replay of real captured validate/demo output), both with reproducible Pillow generator scripts; README banner + Demo section |
| 2026-08-27 | Orchestrator | Reviewed visuals (inspected banner and final GIF frame; verified transcript matches real demo output); committed, pushed, opened PR |
| 2026-08-30 | Orchestrator | Opened roadmap issues #6-#12 (CI, PyPI release, live runs, mixed-target spawning, richer edge semantics, new adapters, quality backlog); added ROADMAP.md linking them; README roadmap section now points at ROADMAP.md; opened PR |
| 2026-08-30 | Sonnet 5 subagent | v0.0.1 release prep: version bump, real build (sdist+wheel, twine check PASSED, fresh-venv core-only install verified), publish.yml (tag-triggered PyPI Trusted Publishing with tag/version guard), minimal ci.yml (core-only tests on push/PR), README/ROADMAP/demo-runs updates |
| 2026-08-30 | Orchestrator | Reviewed release setup: workflows verified, suite reran green (126); committed, pushed, opened PR; tag v0.0.1 to be pushed after merge + PyPI pending-publisher registration |
| 2026-08-30 | Orchestrator | Tag push blocked by branch-scoped git credentials (403); added workflow_dispatch path to publish.yml (publishes pyproject version, then tags from inside Actions); merged and triggered the release run |
| 2026-09-02 | Sonnet 5 subagent | Full CI matrix (core + per-extra matrix + non-blocking all-extras, concurrency group); broader model_params per adapter verified against installed SDKs (incl. per-provider maps for autogen/langgraph where clients silently drop or reroute unmapped keys); `commonadk new <agent>` scaffolding with optional edge wiring; docs updated; 165 tests |
| 2026-09-02 | Orchestrator | Reviewed: verified CI yaml structure, reran suite (165), smoke-tested `commonadk new` end-to-end (scaffold -> validate exit 0, edge appended, overwrite refused); committed and pushed |
| 2026-09-02 | Sonnet 5 subagent | M9 foundation: mixed-target spawning in-process — `runtime:` now honored (real validation errors, no longer a warning), island computation (union-find over same-runtime edges, root must reach all members), each island built natively by its own adapter, cross-runtime edges bridged by plain callables; design doc, mixed-crew example, 17 tests |
| 2026-09-02 | Orchestrator | Reviewed M9 in its worktree (143 passing there; earlier failures were only a stale editable install pointing at the main tree), patched into the branch alongside the CI/model_params work, resolved cleanly, full suite 182 passing |
| 2026-09-02 | Orchestrator | publish.yml: added API-token fallback (PYPI_API_TOKEN secret wins when present, else trusted publishing) after repeated invalid-publisher failures on the PyPI-side record |
| 2026-09-02 | Orchestrator | PR #17 and #18 merged; publish workflow succeeded on re-run — **commonadk 0.0.1 live on PyPI**, v0.0.1 tag pushed by the workflow. Verified from a clean venv: pip install commonadk -> `commonadk --version` 0.0.1 -> validate exit 0. ROADMAP updated (CI matrix, PyPI, mixed-target foundation, model_params/new moved to Shipped) |
| 2026-09-10 | Sonnet 5 subagent | SKILL.md interop: loader now honors skill.md frontmatter (agent-config authoritative, frontmatter fills gaps, name mismatch errors, unknown keys warn) and a new `commonadk import <skills-dir> <common-dir>` turns any SKILL.md library into a conforming project (both layouts, name normalization + collision dedup, no invented edges, extend-or-refuse); 25 new tests |
| 2026-09-10 | Orchestrator | Reviewed in worktree (207 passing with PYTHONPATH=src) and re-verified in the main tree; smoke-imported Spotify's portal-ai-plugins skills/ (6 skills) -> `commonadk validate` exit 0 with descriptions carried from frontmatter; patched onto main, 207 passing; opened PR |
| 2026-09-10 | Orchestrator | Opened #21 (export direction, framed as a demand question) and #22 (execution/telemetry layer) |
| 2026-09-10 | Sonnet 5 subagent | Built the runner/telemetry core (#22) in new src/commonadk/runners/: normalized event model with Optional (never 0) usage, lazy runner registry, sessions, observe-only hooks with isolate-and-report policy, JSON trace with explicit usage_complete/cost_complete flags, static pricing table; google-adk and openai runners implemented against installed SDKs; CLI --stream/--trace; docs/runner-design.md maps all six SDKs; 36 new tests |
| 2026-09-10 | Orchestrator | Reviewed in worktree (243 passing with PYTHONPATH=src), patched onto main, re-verified 243; smoke-checked None-not-0 usage semantics and the unported-target message; opened PR |
| 2026-09-10 | Sonnet 5 subagent | First live-run attempt died on an account rate limit before producing anything; retried. Second run delivered #8: examples/live-smoke (haiku default, single deterministic tool), scripts/live_smoke.py (--list/--dry-run/--targets/--model, real traces for google-adk+openai, explicit "usage: unavailable" for the other four, exit 2 on missing key), .github/workflows/live-runs.yml (dispatch-only, CLAUDE_API_KEY -> ANTHROPIC_API_KEY with detect-in-a-step guard), 12 tests |
| 2026-09-10 | Orchestrator | Integrated selectively -- the agent's worktree lacked the runner layer so it re-extracted those files; copied only the live-run artifacts and verified the three doc files differed purely by live-run content. Suite 255 passing; verified --list, the missing-key guard, and validate on the smoke project |
| 2026-09-10 | Orchestrator + user | Live runs executed for real (workflow run #4, model claude-haiku-4-5): **5 of 6 targets succeeded end-to-end** — google-adk, openai, claude, crewai, langgraph each called the model, invoked the tool and returned the same answer. autogen failed on a verified upstream incompatibility (autogen-ext 0.7.5 injects `temperature` into every Anthropic request; anthropic 1.x removed it from messages.create()). Runs #1-#3 were a dry run, a Codespaces-vs-Actions secret placement error, and an account credit balance failure |
| 2026-09-10 | Sonnet 5 subagent | Captured the real run in docs/demo-runs.md and moved #8 to Shipped in ROADMAP.md, with explicit caveats that autogen is unresolved and openai's 0 tokens/$0 means "not measured", never "free" |
| 2026-09-10 | Orchestrator | Reviewed and integrated the docs capture; fixed the now-false "no captured output has ever come from a real LLM call" claim in README (found by the subagent, outside its scope); 255 tests passing |
| 2026-09-10 | Sonnet 5 subagent | Fixed the openai runner's usage honesty bug the live run exposed: openai-agents synthesizes `Usage(requests=1)` with all-zero tokens when a provider returns no usage (LiteLLM bridge), which the old `requests > 0` heuristic read as "reported", printing a confident 0 tokens/$0. Predicate now keys off the token fields themselves; a genuine zero is provably indistinguishable from an unreported one, so all-zero is treated as unknown and documented as the conservative choice. 256 tests |
| 2026-09-10 | Orchestrator | Reviewed and integrated; verified the old vs new predicate against a real SDK-synthesized Usage object; 256 passing |
| 2026-09-10 | Sonnet 5 subagent | Fixed the autogen/anthropic incompatibility: reproduced the TypeError in a throwaway venv with anthropic 1.5.0 (break is at the 0.125.0 -> 1.0.0 boundary), then shipped pin + guard. Pinned `anthropic<1` in the autogen extra (verified: claude-agent-sdk declares no anthropic dep at all, langchain-anthropic allows >=0.120,<2, so a fresh all-extras install resolves 0.125.0 with pip check clean), plus a runtime guard raising a clear RuntimeError naming both versions and the remedy. Drafted (not filed) an upstream issue. 3 new tests |
| 2026-09-10 | Orchestrator | Reviewed and integrated; verified the guard raises the actionable error under a simulated anthropic 1.5.0; 259 tests passing |
| 2026-09-10 | Orchestrator + user | Live run #5 on merged main: **6 of 6 targets succeeded** — autogen now runs (anthropic<1 pin confirmed in a fresh CI install) and openai reports `?` instead of a confident 0/$0 (honesty fix confirmed in production). crewai 25.52s -> 6.76s between runs, confirming the earlier spread was largely cold-start noise and rightly never published as a benchmark. Captured run #5 as the reference result in docs/demo-runs.md; run #4 kept as the record of what the first real run exposed; ROADMAP #8 updated to all-six |
| 2026-09-11 | Orchestrator | Issue sweep: closed #6 (CI shipped — core + per-extra matrix + non-blocking all-extras); commented status on #7 (release machinery proven, 0.0.1 live on PyPI, held open for the literal 0.1.0 cut gated on #22). Found and fixed a real PyPI defect: README used relative `assets/` image paths, so the banner and demo GIF were broken images on the project page (the long_description is served standalone there) — switched to absolute raw.githubusercontent URLs, which render identically on GitHub |
| 2026-09-11 | Orchestrator | Spawned four Sonnet 5 subagents in isolated worktrees on deliberately disjoint file scopes: (1) claude+crewai runners, (2) autogen+langgraph runners — both #22, split so `runners/__init__.py` is the only shared file and only two registry entries each; (3) #10 first checkbox only (honor delegate vs handoff per target, re-scoped fresh rather than salvaging the stale worktree); (4) #12 checkboxes 1+4 (model_params audit, dependency-pin watch). (3) and (4) share the adapter files but were partitioned by region — edge wiring vs model-param maps. #9 part two (A2A) held back: it touches every adapter and would collide with (3). #11 left for community; #21 remains a demand question, not build work |
| 2026-09-11 | Sonnet 5 subagent | #12 checkbox 1 audited and closed as **already complete** — an honest no-op. The issue text ("today only temperature and max_tokens map") predates commit 2eca52c (2026-09-02), which had already widened every map. The agent re-verified all 8 neutral params across all 6 targets by direct SDK introspection rather than trusting the prior work: nothing had rotted, docs/file-contracts.md still matched, no diff manufactured. Checkbox 4 (pin watch) re-verified: all-extras dry-run resolves, `pip check` shows only the two pre-existing documented tensions, `anthropic<1` still needed (0.122.0's AsyncMessages.create still takes `temperature`). Dated evidence recorded next to the pins |
| 2026-09-11 | Orchestrator | Spot-checked the audit independently instead of accepting it: confirmed openai `ModelSettings` has exactly the 5 claimed fields and genuinely lacks top_k/stop/seed, google's map covers all 8, and claude's `AgentDefinition` has no sampling field at all (so the empty map is correct). Integrated the pin-watch notes. Noted for later: openai `ModelSettings.extra_args` could technically carry stop/seed through to the API, but passing unvalidated kwargs is the silent-swallow risk the project deliberately refuses — left as warn-and-ignore, flagged not acted on |
| 2026-09-11 | Orchestrator | All three remaining subagents killed simultaneously by an account-level session rate limit (429, reset 10:50 UTC) — a capacity failure, not a code failure. All three had substantial uncommitted work in their worktrees; snapshotted each as a WIP commit before touching anything else, then resumed the same agents rather than respawning, so ~1,700 lines of runner code and the partial edge-semantics diff were preserved instead of rewritten. Survived: claude_agent.py (389 lines) + crewai_runner.py (417), autogen.py (402) + langgraph.py (467) + registry/doc edits, and 4 modified adapters + 3 test files for #10. Gaps at kill time: neither runner agent had written tests, the claude/crewai agent had not touched the registry, and #10 had not yet reached claude/crewai/langgraph — langgraph being the one target where the distinction is genuinely expressible |
| 2026-09-11 | Sonnet 5 subagent | #22 autogen + langgraph runners (resumed after the rate-limit kill, work preserved): full BaseRunner contract for both, 10 offline tests, docs/runner-design.md gains real `### AutoGen`/`### LangGraph` sections that **correct** rather than extend the original speculative mapping. Two honest deviations documented: LangGraph handoffs are detected via the `transfer_to_<dest>` tool-name convention (not a dedicated message type, as the design doc had assumed), and a checkpointer cannot be attached to what `project.build()` returns, so session continuity lives in `RunSession` (replayed history) instead of `MemorySaver`. Both runners resolve model/cost per message source rather than once from the build root, since both SDKs carry native per-message attribution |
| 2026-09-11 | Orchestrator | Verified rather than trusted: reran the suite in the worktree (269), then confirmed the central honesty claim against installed SDK source — `autogen_ext/models/openai/_openai_client.py:712` really does default `prompt_tokens` to `0` (not None) when a provider reports no usage, so the runner's `reported = bool(prompt or completion)` predicate is justified and is the same bug shape already fixed for the openai runner. Integrated; 269 passing on the branch. Runner coverage now 4 of 6 — `claude` and `crewai` remain unported |
| 2026-09-11 | Sonnet 5 subagent | #22 claude + crewai runners (resumed): Claude runner uses `ClaudeSDKClient` (not one-shot `query()`) so sessions work, takes `cost_usd` verbatim from `ResultMessage.total_cost_usd` (bypassing pricing.py — the SDK's own figure is more authoritative than a static table) and gets a genuinely SDK-reported `duration_ms`. CrewAI runner discovered mid-build that `crewai_event_bus` exposes **per-call** usage via `LLMCallCompletedEvent`, correcting the design doc's "crew-wide only" plan; delegation turns out to be an ordinary tool call, also visible on the bus. Documented limitation: the bus dispatches on a 10-worker thread pool, so cross-event-kind ordering is not guaranteed. CrewAI `RunSession` raises NotImplementedError — no first-class multi-turn primitive exists |
| 2026-09-11 | Sonnet 5 subagent | #10 delegate/handoff honored on 4 of 6 targets: LangGraph (recursive build + invoke vs `Command(goto, graph=PARENT)`), Google ADK (`AgentTool` vs `sub_agents`), OpenAI Agents (`as_tool` vs `.handoffs`), AutoGen (`AgentTool`/`TeamTool` vs Swarm handoffs). Claude and CrewAI keep the collapsed mapping for a real reason, not a shrug: neither SDK has any transfer-and-never-return primitive, so every edge is already structurally a delegate |
| 2026-09-11 | Orchestrator | **Integration caught a defect neither agent could see.** Each branch was green alone, but both branched from the same main: #10 changes what `build()` returns (a root with only delegate edges now yields a bare agent, not a Swarm/multi-node graph) while the runners consume that shape. Combined, an autogen runner test that patched `Swarm.run_stream` silently missed and reached the real `AssistantAgent` — an offline test attempting a live API call. Fixed at the fixture (the test drives a Swarm by intent, so its edge is now a handoff) rather than by weakening the assertion. Also fixed two stale-by-success tests: live_smoke asserted some target reports "unavailable" (false once all six ported — that was asserting a regression), and two CLI tests plus one vacuous loop over the now-empty `known_unported_targets()` were silently testing nothing; all three now inject a synthetic unported target, which is the real state #11 will reintroduce. Registry conflict resolved to the union of all six; `_UNPORTED_TARGETS` kept as an empty set on purpose. **297 tests, 0 skipped** |
| 2026-09-11 | Orchestrator | Caught a blocker before triggering a live run: `scripts/live_smoke.py` hardcoded `RUNNER_TARGETS = {"google-adk", "openai"}` while `--list` read the registry, so the two had drifted. A live run would have reported the four new runners as `usage: unavailable` and looked green while never exercising them. Now derived from `known_targets()`; `--list` confirms all six report a runner. Third stale-by-success test fixed the same way (`test_report_shape_and_usage_marker_for_all_targets` asserted the four were permanently "unavailable"). ROADMAP updated: #22 and the shipped half of #10 moved to Shipped, with #10's remaining four items kept explicitly open. 297 tests |
| 2026-09-11 | Orchestrator | Opened PR #26 (all six runners + delegate/handoff + the four stale-by-success test fixes), subscribed to its CI. Attempted to dispatch live-runs.yml on the branch and got 403 "Resource not accessible by integration" — this session's GitHub token cannot dispatch workflows, so the live run needs the user. Flagged explicitly rather than reported as done: every change in #26 is verified offline only, and the last live run (#5) predates all of it, so the four new runners' real token/cost reporting is unproven in production |
| 2026-09-11 | Orchestrator | CI red on PR #26: `extras (crewai)`, 17 failures. Established first that it is **red on main too** — same job, same failure, every main run back to #19, while all seven other jobs are green there — so the root cause predates this PR. But 5 of the 17 are this PR's new crewai runner tests tripping the same cause, so the PR cannot go green without fixing it. Root cause: the `crewai` extra declared only `crewai[litellm]`, but crewai routes `gemini/...` and `anthropic/...` to **native** provider clients that litellm never sees, and those import `google-genai`/`anthropic` directly. Masked everywhere except the crewai-only matrix leg, because the `google` extra pulls `google-genai` in transitively via google-adk — precisely the drift that leg exists to catch. Reproduced in a clean venv with only `.[crewai,dev]` (16 failed), installed the two provider extras, re-ran (20 passed), then applied the fix. Also corrected `crewai_adapter.py`'s "Model routing" docstring, whose claim that litellm covers "everything else" is what made the gap invisible |

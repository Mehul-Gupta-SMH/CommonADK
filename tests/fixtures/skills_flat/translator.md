# Translator

Translate the given text into the requested language.

This fixture deliberately has no frontmatter, to exercise the
name-from-filename / no-description fallback path of `commonadk import`.
